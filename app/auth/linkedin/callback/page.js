export const dynamic = "force-dynamic";
export const runtime = "edge"; // optional, but can help

import LinkedInCallback from "@/components/LinkedInCallback";
import React from "react";

const page = () => {
  return <LinkedInCallback />;
};

export default page;
